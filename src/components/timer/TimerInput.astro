---
export type Props = {
  classId: string;
};

const { classId } = Astro.props;
---

<input
	class:list={[
		"w-[2ch] bg-base-200 rounded-lg",
		'outline-none focus:bg-opacity-70 focus:bg-base-content focus:text-base-300',
		classId
	]}
	value="00"
	type="text"
/>
